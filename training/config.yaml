model:
  base_model: mistralai/Mistral-7B-Instruct-v0.2
  max_seq_length: 1024

training:
  epochs: 3
  learning_rate: 2e-4
  batch_size: 4
  grad_accum_steps: 4

lora:
  r: 16
  alpha: 32
  target_modules:
    - q_proj
    - v_proj
